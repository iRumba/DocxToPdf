# Секреты проекта

На данный момент проект не использует внешние сервисы, требующие секретов.
Все конфигурации выполняются через переменные окружения в Docker Compose.

| Переменная | Назначение | Источник |
|-----------|------------|---------|
| — | — | — |
