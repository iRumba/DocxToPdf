# Инструменты проекта

## Docker

- **Версия:** {{docker_version}}
- **Назначение:** Контейнеризация приложения и инфраструктуры
- **Файлы:**
  - `Dockerfile` — сборка приложения
  - `docker-compose.yml` — локальный запуск (приложение + зависимости)

### Локальный запуск

```bash
docker compose up -d
```

### Сборка образа

```bash
docker build -t {{image_name}} .
```

---

## {{tool_name}} (например, Kafka)

- **Версия:** {{tool_version}}
- **Назначение:** {{tool_purpose}}
- **Конфигурация:** {{tool_config_details}}

### Как используется в проекте

{{tool_usage_description}}

### Локальный запуск

{{tool_local_run_instructions}}

---

## {{tool_name_2}} (например, Redis)

...

---

*Для каждого инструмента указаны версии, способ запуска и особенности использования на проекте.*
*Секреты для инструментов — в [secrets.md](secrets.md).*
