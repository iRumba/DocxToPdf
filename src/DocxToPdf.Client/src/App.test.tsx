import { describe, it, expect } from "vitest";

// Simple placeholder test — vitest is configured
describe("App", () => {
  it("should render without crashing (placeholder)", () => {
    expect(true).toBe(true);
  });
});
